# Versão 1.9.1 - Correção de Caminhos de Fontes

**Data de Lançamento:** 31 de dezembro de 2025  
**Tipo de Atualização:** PATCH (Correção de bug)

---

## 📋 Resumo das Alterações

Esta versão corrige um erro crítico de carregamento de fontes (404 Not Found) causado por caminhos incorretos e duplicados nos arquivos CSS das fontes locais.

---

## 🐛 Correções Implementadas

### 1. Ajuste de Caminhos Relativos
Foi identificado que os arquivos CSS dentro de `database/fonts/[familia]/` estavam tentando carregar os arquivos `.woff2` usando caminhos que duplicavam o diretório `database/` ou apontavam para locais inexistentes.

Os caminhos foram corrigidos para serem **relativos ao próprio arquivo CSS**, garantindo que o navegador localize corretamente os arquivos de fonte independentemente de onde o CSS principal seja carregado.

### 2. Remoção de Duplicação
Removida a string `../../database/fonts/` que estava sendo prefixada incorretamente nos arquivos de definição de fontes individuais, o que causava URLs inválidas como `database/database/fonts/...`.

---

## 📂 Arquivos Modificados

- Todos os arquivos `.css` dentro de subpastas em `database/fonts/` (ex: `righteous.css`, `montserrat.css`, `inter.css`, etc.)
- `src/app/version.json` (Versão atualizada para 1.9.1)
- `src/app/update.js` (CURRENT_VERSION atualizado para 1.9.1)
- `src/app/versions/README.md` (Índice atualizado)

---

## 🎯 Benefícios para o Usuário

Esta correção restaura a identidade visual completa do aplicativo, garantindo que todas as fontes personalizadas (Righteous, Montserrat, Poppins, Inter, etc.) sejam carregadas corretamente. Isso elimina os erros no console do navegador e assegura que o design "Liquid" seja exibido exatamente como planejado, mesmo em modo offline.

---

## 🔧 Detalhes Técnicos

### Lógica de Correção
A correção foi aplicada em massa usando scripts de shell para garantir consistência em todas as famílias de fontes. A estratégia adotada foi transformar caminhos absolutos/incorretos em caminhos relativos diretos (ex: `url(arquivo.woff2)`), que é a prática recomendada quando o recurso está na mesma pasta que o CSS.

### Verificação de Integridade
Após a aplicação dos scripts, os arquivos foram verificados manualmente para confirmar que a sintaxe `url(nome-do-arquivo.woff2)` foi preservada corretamente em todas as declarações `@font-face`.

---

**Desenvolvido com foco em estabilidade, fidelidade visual e correção técnica.** 🚀
